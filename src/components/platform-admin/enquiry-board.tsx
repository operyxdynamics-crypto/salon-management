/**
 * Dead file - safe to delete.
 *
 * The enquiry board was replaced by the pipeline board (`pipeline-board.tsx`) when Enquiries became
 * Pipeline. Nothing imports this module; it is kept only because the file could not be deleted from
 * the automated session. Delete it whenever convenient.
 */
export {};
